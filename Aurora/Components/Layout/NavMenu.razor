<nav class="aurora-topnav">
    

    <div class="nav-right">
        <NavLink href="" Match="NavLinkMatch.All">
            <span class="icon">
                <svg width="18" height="18" viewBox="0 0 24 24" fill="none">
                    <path d="M3 9.5L12 3L21 9.5V21H3V9.5Z" stroke="#c6e7ff" stroke-width="2" />
                </svg>
            </span>
            Home
        </NavLink>

        <NavLink href="songs">
            <span class="icon">
                <svg width="18" height="18" viewBox="0 0 24 24" fill="none">
                    <path d="M9 18V5L21 3V16" stroke="#c6e7ff" stroke-width="2" />
                    <circle cx="6" cy="18" r="3" stroke="#c6e7ff" stroke-width="2" />
                    <circle cx="18" cy="16" r="3" stroke="#c6e7ff" stroke-width="2" />
                </svg>
            </span>
            Songs
        </NavLink>

        <NavLink href="playlists">
            <span class="icon">
                <svg width="18" height="18" viewBox="0 0 24 24" fill="none">
                    <rect x="3" y="5" width="18" height="14" rx="2" stroke="#c6e7ff" stroke-width="2" />
                    <path d="M7 9H17" stroke="#c6e7ff" stroke-width="2" />
                </svg>
            </span>
            Playlists
        </NavLink>

        <NavLink href="ratings">
            <span class="icon">
                <svg width="18" height="18" viewBox="0 0 24 24" fill="none">
                    <path d="M12 3L14.6 9.2L21 10L16 14L17.2 20.4L12 17L6.8 20.4L8 14L3 10L9.4 9.2L12 3Z" stroke="#c6e7ff" stroke-width="2" />
                </svg>
            </span>
            Ratings
        </NavLink>

        <NavLink href="account">
            <span class="icon">
                <svg width="18" height="18" viewBox="0 0 24 24" fill="none">
                    <circle cx="12" cy="8" r="4" stroke="#c6e7ff" stroke-width="2" />
                    <path d="M4 21C4.8 17 8 14 12 14C16 14 19.2 17 20 21" stroke="#c6e7ff" stroke-width="2" />
                </svg>
            </span>
            Account
        </NavLink>
    </div>
</nav>

<style>
    .aurora-topnav {
        position: sticky;
        top: 0;
        display: flex;
        justify-content: space-between;
        align-items: center;
        padding: 0.8rem 1.8rem;
        background: rgba(10, 13, 30, 0.75);
        backdrop-filter: blur(16px);
        border-bottom: 1px solid rgba(120, 200, 255, 0.18);
        z-index: 1000;
        box-shadow: 0 6px 18px rgba(0, 0, 0, 0.4);
        animation: fadeDown 0.6s ease-out;
    }

    @@keyframes fadeDown {
        from {
            opacity: 0;
            transform: translateY(-12px);
        }

        to {
            opacity: 1;
            transform: translateY(0);
        }
    }

    .aurora-topnav .nav-left {
        display: flex;
        align-items: center;
        gap: 1.2rem;
    }

    .aurora-topnav .logo {
        font-size: 1.6rem;
        font-weight: 800;
        background: linear-gradient(90deg, #78e3ff, #d3a2ff);
        -webkit-background-clip: text;
        color: transparent;
        letter-spacing: 1.5px;
        text-shadow: 0 0 14px rgba(140, 230, 255, 0.45);
    }

    .aurora-topnav .login-btn {
        background: linear-gradient(90deg, #2193b0, #6dd5ed);
        padding: 0.45rem 0.95rem;
        border-radius: 8px;
        font-weight: 600;
        text-decoration: none;
        transition: transform 0.2s, opacity 0.2s, box-shadow 0.3s;
        box-shadow: 0 0 12px rgba(100, 200, 255, 0.25);
    }

        .aurora-topnav .login-btn:hover {
            transform: translateY(-1px) scale(1.05);
            opacity: 0.95;
            box-shadow: 0 0 16px rgba(120, 220, 255, 0.45);
        }

    .aurora-topnav .nav-right {
        display: flex;
        gap: 1.4rem;
    }

    .aurora-topnav a {
        display: flex;
        align-items: center;
        gap: 0.45rem;
        font-size: 1rem;
        padding: 0.55rem 0.8rem;
        border-radius: 10px;
        color: #e4f4ff;
        text-decoration: none;
        font-weight: 500;
        position: relative;
        transition: 0.25s cubic-bezier(0.22, 0.61, 0.36, 1);
    }

        .aurora-topnav a:hover {
            background: rgba(100, 200, 255, 0.15);
            box-shadow: 0 0 12px rgba(130, 220, 255, 0.25);
            transform: translateY(-2px);
        }

        .aurora-topnav a.active {
            background: linear-gradient(90deg, #6dd5ed, #2193b0);
            color: white;
            font-weight: 700;
            box-shadow: 0 0 18px rgba(100, 200, 255, 0.4);
        }

        .aurora-topnav a .icon svg {
            filter: drop-shadow(0 0 4px rgba(150, 220, 255, 0.4));
        }

        /* subtle glow underline on hover */
        .aurora-topnav a::after {
            content: "";
            position: absolute;
            bottom: 4px;
            left: 50%;
            width: 0;
            height: 2px;
            background: linear-gradient(90deg, #78e3ff, #d3a2ff);
            transition: width 0.25s ease, left 0.25s ease;
            border-radius: 10px;
        }

        .aurora-topnav a:hover::after {
            width: 70%;
            left: 15%;
        }
</style>
